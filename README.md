# AI Script & Social Generator

Generate engaging scripts and platform-tailored social media posts from a single topic.

## Running locally

```bash
npm install
npm run dev
```

Set `OPENAI_API_KEY` for best results. Without it, a high-quality fallback template is used.

## Deploying

```bash
vercel deploy --prod --yes --name agentic-70ff3b80
```
